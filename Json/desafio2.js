import { FiEdit3, FiTrash } from 'react-icons/fi';
import { Container } from './CSS/Style.css';
let formularios = {
    vendedor: "Sandra",
    edad: 35,
    ocupacion: Gestor
  };
  const setearPropiedad = (propiedad, valor) => {
    persona[propiedad] = valor;
  };
  const papel = {
    tipo: "04",
    vehiculo: "Auto",
  };
  console.log(vendedor, edad, ocupacion);